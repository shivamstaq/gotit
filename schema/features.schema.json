{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/shivamstaq/gotit/schemas/features.v1.json",
  "title": "gotit Features Allowlist (v1)",
  "description": "Schema for the feature-flag file consumed by gotit. Each entry maps a flag name to a boolean indicating whether the feature has shipped.",
  "type": "object",
  "required": ["enabled"],
  "additionalProperties": false,
  "properties": {
    "enabled": {
      "type": "object",
      "additionalProperties": { "type": "boolean" },
      "propertyNames": { "pattern": "^[a-z][a-z0-9-]*$" }
    }
  }
}
